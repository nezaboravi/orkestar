{
  "name": "agent-orchestra",
  "version": "0.1.0",
  "private": true,
  "type": "module",
  "bin": {
    "lenka": "./lenka.mjs"
  },
  "scripts": {
    "bootstrap:check": "node --test tests/bootstrap.test.mjs",
    "doctor": "node orchestra.mjs doctor",
    "test": "node --test tests/*.test.mjs"
  },
  "engines": {
    "node": ">=20"
  }
}
