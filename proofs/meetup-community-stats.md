# Community stats — small-feature meetup rehearsal

This extends the existing Meetup Picks application. It is a 15-minute target,
not a measured guarantee. The previous search rehearsal took 31m56s and ended
PARTIAL. Do not present this replacement as timed proof until it passes a real
rehearsal. Do not create a new Laravel project or delete previous work.

## Before starting

Use the updated Orkestar installation and a fresh Lenka session in the existing
project. Keep the existing app, seeded data and development server. Confirm
Taskavel and browser access before presenting live; setup time is not feature
implementation time and must be disclosed. Do not silently continue the prior
unfinished search contract or close its tracker work.

## Paste into Lenka

Add a compact community stats strip immediately above the ranked proposals on
this existing Meetup Picks page: **N proposals · M total votes**. Use the
existing typography, colors and spacing; this placement and design direction
are approved. Do not redesign the page.

Calculate both numbers from the complete `proposals` data already supplied to
the page: array length and the sum of `votesCount`. They must not change when
the user searches or clears the search. Derive them reactively so fresh page
data updates the totals. Use correct singular/plural labels. Preserve proposal
ranking, search, submission, voting and authentication exactly as they are.

This is a new, bounded task, not a continuation or approval of earlier work.
No database changes, new requests, dependencies, reseeding, new pages, commits,
deployment or publication. Use existing data; never reset the app.

Proceed with a compact plan you write yourself. Before coding, create exactly
one task in the existing bound Taskavel project and verify its link and In
Progress state. If that fails, report the blocker before starting paid builders;
do not spend the demonstration debugging authentication.

Use one visible builder. Finish affected-file formatting before handing off
the final diff. Then start focused testing, browser QA and independent
security/performance review in parallel. Pass each only the small contract,
changed files and relevant evidence. No planner/designer session, repeated
repository inventories or unrelated full-suite audits are needed for this
specified change. Never use reasoning above high.

Prove the displayed totals match the existing complete dataset, remain unchanged
when search narrows the cards and when cleared, and fit desktop and mobile
without overflow. Check the relevant browser console/network results. The
reviewer must inspect the actual diff, including text escaping, reactive work,
and absence of new network/database operations. Have the independent auditor
accept the feature evidence. Move the Taskavel task through Review & QA to
Done only after acceptance; verify both column and completion state. Tracker
closure must not restart an already-passed code review.
Keep feature acceptance criteria separate from the required tracker reporting
gate: the auditor accepts application behavior, then runtime-owned tracker
readback must prove closure before the overall report can say DONE.

Show readable model-and-role worker names, one outcome todo, and concise team
and final reports. Use the managed team overview; do not manually create one
scratchpad per worker. End with the Taskavel link, verdict, behavior proof,
actual worker/model/usage evidence and elapsed time. Save a local handoff.

Target 15 minutes. At 10 minutes, report the exact remaining checks without
adding scope. At 15 minutes, report DONE only if all required evidence and
tracker closure are complete; otherwise report PARTIAL and ask whether to
continue. Do not kill workers, discard work or skip review to fit the timer.
